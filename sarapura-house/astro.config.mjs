import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://sarapura.house',
  // Si vas a deployar bajo un subpath (ej. GitHub Pages /sarapura-house),
  // descomentá y ajustá la siguiente línea:
  // base: '/sarapura-house',
});
